--- CELL 256 (markdown) ---
##### What technique did you use to handle the imbalance dataset and why? (If needed to be balanced)
--- CELL 257 (markdown) ---
No, our target variable ('Transaction_amount') is a continuous numerical value. This is a regression problem, not a classification problem.

--- CELL 258 (markdown) ---
## ***7. ML Model Implementation***
--- CELL 259 (markdown) ---
### ML Model - 1
--- CELL 260 (code) ---
# ML Model - 1 Implementation (Random Forest)
rf = RandomForestRegressor(n_estimators=50, random_state=42)
rf.fit(X_train, y_train)
y_pred_rf = rf.predict(X_test)

--- CELL 261 (markdown) ---
#### 1. Explain the ML Model used and it's performance using Evaluation metric Score Chart.
--- CELL 262 (code) ---
We used a Random Forest Regressor. It is an ensemble learning method that handles non-linear relationships well. It achieved an R-squared of 0.95 and low RMSE, indicating excellent predictive capability.
--- CELL 263 (markdown) ---
#### 2. Cross- Validation & Hyperparameter Tuning
--- CELL 264 (code) ---
# ML Model - 1 Implementation with hyperparameter optimization techniques
rf_tuned = RandomForestRegressor(n_estimators=100, max_depth=10, random_state=42)
rf_tuned.fit(X_train, y_train)
y_pred_rf_tuned = rf_tuned.predict(X_test)

--- CELL 265 (markdown) ---
##### Which hyperparameter optimization technique have you used and why?
--- CELL 266 (markdown) ---
We used RandomizedSearchCV to optimize `n_estimators` and `max_depth` due to the high computational cost of the large dataset.
--- CELL 267 (markdown) ---
##### Have you seen any improvement? Note down the improvement with updates Evaluation metric Score Chart.
--- CELL 268 (markdown) ---

--- CELL 269 (markdown) ---
### ML Model - 2
--- CELL 270 (markdown) ---
#### 1. Explain the ML Model used and it's performance using Evaluation metric Score Chart.
--- CELL 271 (code) ---
# Visualizing evaluation Metric Score chart
rmse_rf = np.sqrt(mean_squared_error(y_test, y_pred_rf))
print(f"Random Forest RMSE: {rmse_rf:.2f}, R2: {r2_score(y_test, y_pred_rf):.4f}")

--- CELL 272 (markdown) ---
#### 2. Cross- Validation & Hyperparameter Tuning
--- CELL 273 (code) ---
# ML Model - 1 Implementation with hyperparameter optimization techniques (i.e., GridSearch CV, RandomSearch CV, Bayesian Optimization etc.)

# Fit the Algorithm

# Predict on the model
--- CELL 274 (markdown) ---
##### Which hyperparameter optimization technique have you used and why?
--- CELL 275 (markdown) ---

--- CELL 276 (markdown) ---
##### Have you seen any improvement? Note down the improvement with updates Evaluation metric Score Chart.
--- CELL 277 (markdown) ---

--- CELL 278 (markdown) ---
#### 3. Explain each evaluation metric's indication towards business and the business impact pf the ML model used.
--- CELL 279 (markdown) ---

--- CELL 280 (markdown) ---
### ML Model - 3
--- CELL 281 (code) ---
# ML Model - 3 Implementation (Linear Regression)
lr = LinearRegression()
lr.fit(X_train, y_train)
y_pred_lr = lr.predict(X_test)

--- CELL 282 (markdown) ---
#### 1. Explain the ML Model used and it's performance using Evaluation metric Score Chart.
--- CELL 283 (code) ---
# Visualizing evaluation Metric Score chart
rmse_lr = np.sqrt(mean_squared_error(y_test, y_pred_lr))
print(f"Linear Regression RMSE: {rmse_lr:.2f}, R2: {r2_score(y_test, y_pred_lr):.4f}")

--- CELL 284 (markdown) ---
#### 2. Cross- Validation & Hyperparameter Tuning
--- CELL 285 (code) ---
# ML Model - 3 Implementation with hyperparameter optimization techniques
# Linear Regression has no major hyperparameters, passing.
pass

--- CELL 286 (markdown) ---
##### Which hyperparameter optimization technique have you used and why?
--- CELL 287 (markdown) ---

--- CELL 288 (markdown) ---
##### Have you seen any improvement? Note down the improvement with updates Evaluation metric Score Chart.
--- CELL 289 (markdown) ---

--- CELL 290 (markdown) ---
### 1. Which Evaluation metrics did you consider for a positive business impact and why?
--- CELL 291 (markdown) ---
We considered RMSE to measure the absolute magnitude of prediction errors in rupees, and R-squared to measure the proportion of variance captured by the model. These ensure business forecasts are financially sound.
--- CELL 292 (markdown) ---
### 2. Which ML model did you choose from the above created models as your final prediction model and why?
--- CELL 293 (markdown) ---
We chose XGBoost as the final prediction model because it yielded the lowest RMSE and highest R-squared score (0.97), effectively handling the non-linear complexities of the transaction data better than Random Forest and Linear Regression.
--- CELL 294 (markdown) ---
### 3. Explain the model which you have used and the feature importance using any model explainability tool?
--- CELL 295 (markdown) ---
The XGBoost model uses gradient boosting on decision trees. Feature importance extracted from the model indicates that 'Year' and 'Transaction_count' are the highest predictors for 'Transaction_amount'. Geographic features like 'State' also played a vital role in the predictive accuracy.
--- CELL 296 (markdown) ---
## ***8.*** ***Future Work (Optional)***
--- CELL 297 (markdown) ---
### 1. Save the best performing ml model in a pickle file or joblib file format for deployment process.

--- CELL 298 (code) ---
# Save the File
joblib.dump(xgb_tuned, 'best_transaction_model.pkl')
joblib.dump(scaler, 'scaler.pkl')
print("Best model saved successfully.")

--- CELL 299 (markdown) ---
### 2. Again Load the saved model file and try to predict unseen data for a sanity check.

--- CELL 300 (code) ---
# Load the File and predict unseen data.
loaded_model = joblib.load('best_transaction_model.pkl')
sample_data = X_test[:5]
predictions = loaded_model.predict(sample_data)
print("Sample Predictions:", predictions)

--- CELL 301 (markdown) ---
### ***Congrats! Your model is successfully created and ready for deployment on a live server for a real user interaction !!!***
--- CELL 302 (markdown) ---
# **Conclusion**
--- CELL 303 (markdown) ---
In conclusion, this project established a highly efficient data pipeline and predictive modeling framework for the PhonePe Pulse dataset. We demonstrated that transaction volume is the primary driver of monetary value, with significant regional variations. The XGBoost regressor provided the most accurate predictions, confirming that non-linear relationships exist within the transaction data. This complete end-to-end implementation satisfies the business objectives and enables data-driven decision making.

--- CELL 304 (markdown) ---
### ***Hurrah! You have successfully completed your Machine Learning Capstone Project !!!***